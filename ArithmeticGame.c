#include <stdio.h>
#include <stdlib.h>

int main()
{
    int score = 0;
    int minInt = 2, maxInt = 100;
    printf("What is the addition range?\n");
    printf("Lower limit: ");
    scanf("%d", minInt);
    printf("Upper limit: ");
    scanf("%d", maxInt);
    return 0;
}